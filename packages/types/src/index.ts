/**
 * @repo/types
 *
 * Shared TypeScript type definitions used across api and web.
 * This package is TYPES ONLY — no runtime code, no dependencies with side effects.
 *
 * Modules added here as the platform is built:
 *   - common.types.ts     → Pagination, ApiResponse, etc.
 *   - candidate.types.ts
 *   - vendor.types.ts
 *   - jd.types.ts
 *   - submission.types.ts
 *   - interview.types.ts
 *   - auth.types.ts
 *   - ...
 */

// Placeholder export — replaced when types are defined in Step 2+
export type Placeholder = never;
