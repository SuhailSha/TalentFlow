/**
 * @repo/validators
 *
 * Shared Zod validation schemas used for input validation on both
 * the API (DTOs) and web (form validation).
 *
 * Modules added here as the platform is built:
 *   - auth.schema.ts
 *   - candidate.schema.ts
 *   - vendor.schema.ts
 *   - jd.schema.ts
 *   - submission.schema.ts
 *   - pagination.schema.ts
 *   - ...
 */

// Placeholder export — replaced when schemas are defined in Step 2+
export const placeholder = undefined;
